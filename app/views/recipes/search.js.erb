<% if(!@posts.blank?) %>
var postsList = '<%= j render partial: "recipes/recipe_list", locals: {collection: @posts, slugs: @tags} %>\n';
<% end %>
<%= render :partial => "shared/posts_search", locals: {search_path: "recipe_search_path", top_element: "#recipes"} %>
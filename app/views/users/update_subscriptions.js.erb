iziToast.destroy();

<% if @notifications %>
  <% @notifications.each do |n| %>
    iziToast.<%=n.type%>({
        title: '<%= n.title %>',
        message: '<%= n.message.html_safe %>',
        target: '#notifications',
        timeout: <%= n.timeout %>
    });
  <% end %>
<% end %>
<div class="grid">
  <% @recipes.each do |recipe| %>
    <div class="grid-item">
      <%= render :partial => "recipes/recipe_card", locals: {recipe: recipe, card_width: '20rem;', detail_view: false} %>
    </div>
  <% end %>
</div>